# Development image — runs Next dev server with hot reload. Source is
# bind-mounted by compose so edits reflect instantly.
FROM node:24-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci

COPY . .

EXPOSE 3000
CMD ["npm", "run", "dev"]
