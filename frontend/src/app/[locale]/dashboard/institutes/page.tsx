'use client';

import { SuperAdminView } from '@/features/dashboard/super-admin-view';

/** Super-admin institutes management (list + create with manager). */
export default function InstitutesPage() {
  return <SuperAdminView />;
}
